*1.1-1* Describe your own real-world example that requires sorting. Describe one that requires finding the shortest distance between two points.

_having a group of people stand one after another in a height-descending (or ascending) order._

_as perpendicular line is the shortest distance from one point to another on a plane, finding it is equivalent to finding the shortest distance._

*1.1-2* Other than speed, what other measures of efficiency might you need to consider in a real-world setting?

_memory usage, energy efficiency, code readability, amount of time needed to implement an algorithm._

*1.1-3* Select a data structure that you have seen, and discuss its strengths and limitations.

_array allows for instantenous random access, but given its contiguous memory layout, it lacks for fast insertion._

*1.1-4* How are the shortest-path and traveling salesperson problems given above similar? How are they different?

_both of the problems require finding the shortest path between two points, but in traveling salesperson problem, it has to be done for more than one pair of points._

*1.1-5* Suggest a real-world problem in which only the best solution will do. Then come up with one in which "approximately" the best solution is good enough.

_whether or not to give a patient treatment based on an algorithmically processed data._

_whether or not to buy certain stock is an example of a problem in which "approximately" is not only good enough, it is the best one could hope for._

*1.1-6* Describe a real-world problem in which sometimes the entire input is available before you need to solve the problem,
but other times the input is not entirely available in advance and arrives over time.

_to have some data in a sorted order might mean sorting the entire input or implementing a data structure that allows for fast insertion in order to maintain sorted invariant on gradually arriving data_
